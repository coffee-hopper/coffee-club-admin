import { Package, ShoppingCart, FileText, Users, BugIcon } from "lucide-react";

export const sidebarIcons = {
  orders: ShoppingCart,
  invoices: FileText,
  products: Package,
  users: Users,
  tests: BugIcon,
};
